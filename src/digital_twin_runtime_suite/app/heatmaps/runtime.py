"""
Thin facade, similar to Streamlines:

discover
bind
enable
disable
refresh from telemetry
change view/metric
cleanup

This is the module that should later be connected to the existing
RuntimeController.

It owns orchestration, but not the implementation of everything itself.
"""
